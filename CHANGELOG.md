# 变更日志

## 1.6.0（build 13）

### 新功能

- **多抽屉分组**：头部切换/新建/重命名/删除分组（计数徽章）；每组独立排序与容量上限；拖入粘贴进当前分组；⌘1–⌘9 快速切组
- **多选批量**：⌘/⇧点击多选、⌘A 全选；Delete/⌘C/⏎/右键按访达语义作用于整批；多选时拖动瓷片整批拖出（NSDraggingSession，×N 角标预览）
- **置顶与手动排序**：右键置顶（免于自动清理、浮到最前）；行内拖拽排序（把手 + 自定义 UTType 区分内部排序与外部拖入）；⌘↑⌘↓ 快捷键
- **失效实时检测**：启动/展开/列表变化后台扫描；失效行降透明+红色提示；打开自动拦截
- **条目重命名**：右键改名（同目录、同名自动序号、类型重识别）
- **打开方式子菜单**：列出所有可用应用，默认排最前
- **菜单栏快速访问**：最近条目（前 6 个）、导出当前分组、打开收件箱文件夹
- **Spotlight 内容搜索**：名称未命中时检索文件内容补充结果（防抖/超时/转义，设置可关）
- **kind: 搜索语法**：`kind:图片`/`kind:code` 中英文别名类型过滤
- **filedrawer:// URL 自动化**：11 类动作（add/reveal/remove/clear/pin/unpin/send-to-front/move/rename/toggle/expand/collapse），支持 `group=`/`limit=` 参数
- **快捷指令（Shortcuts）**：9 个 App Intents + Siri 短语；与 URL 共用命令层
- **界面语言中英切换**：设置切换即时生效；英文覆盖全部 208 个 UI 字符串 + App Intents 面板
- **WCAG 对比度保障**：瓷片符号色按明暗模式自动调整（≥3:1），设置·外观有色样预览
- **多显示器**：可选「展开时停靠到鼠标所在屏幕」
- **缩略图磁盘缓存**：重启不再重复解码图片/视频/PDF（指纹键 + LRU 淘汰）
- **⌘↵ 预览态打开**：预览打开时用默认应用打开当前条目
- **预览态 Delete 轮播**：移除当前条目并自动切到下一条继续预览

### 质量与基础设施

- 持久化 v3 版本化容器（items/drawers/current/容量覆盖 单 key 原子写，v1/v2 自动迁移）
- 轻量诊断日志（内存环形 200 条 + os_log 双写，菜单栏可导出）
- CI 零警告门禁 + workflow_dispatch 自动化冒烟（8 动作 × 断言）
- 落盘 150ms 防抖（高频变更合并编码），退出即 flush
- VoiceOver 全面修复：图标按钮标签/隐形焦点消除/toast 主动播报
- 性能基准（编码/管线/指纹/淘汰）+ 落盘防抖
- 设置窗口可缩放 + 最小尺寸 460×400（小屏适配）
- 热键注册失败诊断日志 + 用户可见提示
- 4 轮 code-review + 1 轮 VoiceOver 审计，修复 15 个真实缺陷
- README 文件级对账（27/27 一致）+ 实现要点 12 条 + 公证就绪 7 步清单

## 1.1.0

- 收件箱物化文本/链接为真实文件
- 剪贴板互通（⌘C/⌘V）
- 移除可撤销
- 登录启动与 Dock 图标开关
- 350+ 文件类型专属图标
- PDF 缩略图

## 1.0.0

- 初始版本
